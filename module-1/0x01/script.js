#!/usr/bin/node
const firstNumber = window.prompt("Enter your firstNumber")
const secondNumber = window.prompt("Enter your secondNumber")
const sum = parseInt(firstNumber) + parseInt(secondNumber)
alert(`the sum of two numbers is: ${sum}`)